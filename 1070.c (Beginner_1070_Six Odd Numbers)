#include <stdio.h>

int main() {
    int X, count = 0;
    scanf("%d", &X);

    for (int i = X; count < 6; i++) {
        if (i % 2 != 0) {
            printf("%d\n", i);
            count++;
        }
    }

    return 0;
}
